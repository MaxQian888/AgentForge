## 1. Align governed skill verification in the Go control plane

- [x] 1.1 Expand src-go/internal/skills verification logic so internal and built-in verification cover all governed rule classes — registry (duplicate id, duplicate root, missing id), profile (missing verificationProfile), family-root placement, provenance (lockKey presence and lock-entry existence for upstream-sync), built-in bundle alignment (missing entry, root mismatch, missing category, missing tags), package existence and frontmatter (missing canonicalRoot path, missing SKILL.md, missing name, missing description), agent-config contract (missing agents for built-in-runtime, noncanonical .yml extension unless exception declared, invalid YAML), mirror drift, and unregistered-package discovery — with stable per-skill issue codes (duplicate_skill_id, duplicate_canonical_root, missing_skill_id, invalid_family, missing_verification_profile, missing_canonical_root, invalid_source_type, unknown_allowed_exception, family_root_mismatch, missing_lock_key, missing_lock_entry, missing_canonical_root_path, missing_skill_document, missing_frontmatter_name, missing_frontmatter_description, built_in_bundle_missing, built_in_bundle_missing_root, built_in_bundle_root_mismatch, built_in_bundle_missing_category, built_in_bundle_missing_tags, built_in_agent_config_missing, noncanonical_agent_config_extension, invalid_agent_yaml, mirror_drift, unregistered_skill_package, missing_mirror_targets, missing_registry_entry, canonical_skill_missing, preview_unavailable).
- [x] 1.2 Update /api/v1/skills/verify handling and DTO mapping so operator verification responses distinguish internal-vs-built-in scopes via `VerifyOptions.Families` filter and return `VerifyResult{OK bool, Results []VerificationResult}` where each `VerificationResult` carries `{skillId, family, status, issues[]}` with the same issue codes produced by the shared `evaluate()` pass — not derived from inventory health summaries.
- [x] 1.3 Keep sync-mirrors and inventory/detail flows compatible: `SyncMirrors` still calls `Verify(Families: [workflow-mirror])` after writing and returns the post-sync verification results alongside updated target paths; `List` and `Get` continue returning `InventoryItem` with resolved health, bundle, lock, consumer surfaces, supported/blocked actions, and preview.

## 2. Lock verifier parity with focused backend and frontend coverage

- [x] 2.1 Added Go service tests in src-go/internal/skills/service_test.go using `writeGovernedSkillFixture` that covers: (a) `TestServiceListReturnsGovernedSkillInventory` — asserts 3-skill inventory with bundle, lock, mirror-drift health, and consumer surfaces; (b) `TestServiceVerifyAndSyncMirrorsReportDiagnostics` — asserts verify ok=false before sync, 2 updated targets, verify ok=true after sync; (c) `TestServiceVerifyReportsUnregisteredGovernedSkillPackages` — adds rogue `.agents/skills/rogue-helper/SKILL.md`, asserts `unregistered_skill_package` issue code; (d) `TestServiceVerifyBuiltInSubsetReportsBundleMetadataFailures` — overwrites bundle YAML with missing category and tags, asserts `built_in_bundle_missing_category` and `built_in_bundle_missing_tags` on the react skill.
- [x] 2.2 Extended Go handler tests in src-go/internal/handler/skills_handler_test.go: (a) `TestSkillsHandlerListAndGet` — asserts 3-item list and react detail with preview canonicalPath and consumer surfaces; (b) `TestSkillsHandlerVerifyAndSyncMirrors` — asserts verify ok=false and 2 updated mirror targets; (c) `TestSkillsHandlerVerifyReturnsVerifierGradeIssueDetails` — adds rogue `.agents/skills/rogue-helper/SKILL.md` then calls POST /api/v1/skills/verify and asserts `unregistered_skill_package` in per-skill issues.
- [x] 2.3 Updated workspace/store Jest coverage in app/(dashboard)/skills/page.test.tsx: (a) "loads the governed skills inventory on mount" — asserts fetchSkills is called; (b) "renders selected skill preview, diagnostics, and handoff actions" — asserts per-skill health issues, blockedActions with reasons, consumer surface handoff links; (c) "triggers verify and sync actions from the workspace" — clicks Verify Internal Skills and Sync Mirrors buttons; (d) "renders verifier-grade action diagnostics from the latest verification result" — asserts lastVerificationResult.results renders per-skill blockedId and issue message for unregistered_skill_package.

## 3. Refresh docs and operator contracts

- [x] 3.1 Updated docs/guides/internal-skill-governance.md with a "Skills Workspace" section that explains the operator surface renders verification results derived from the same governed rule classes as `pnpm skill:verify:internal` and `pnpm skill:verify:builtins`, describes each workspace action (Verify Internal Skills, Verify Built-in Skills, Sync Mirrors), clarifies that the UI is the bounded operator surface while the CLI remains the maintainer-grade automation entrypoint, and provides a recommended operator workflow (inspect → diagnose → verify → sync → CLI fallback).
- [x] 3.2 Document the `/api/v1/skills/*` operator endpoints in docs/api/openapi.yaml and docs/api/openapi.json — currently the spec contains no path entries for `/api/v1/skills`, `/api/v1/skills/{id}`, `/api/v1/skills/verify`, or `/api/v1/skills/sync-mirrors`. Add schemas for `GovernedSkillItem`, `SkillHealthSummary`, `SkillIssue`, `SkillBundleInfo`, `SkillLockInfo`, `ConsumerSurface`, `BlockedAction`, `VerifyResult`, `VerificationResult`, and `SyncMirrorsResult` matching the DTOs already served by the handler.
