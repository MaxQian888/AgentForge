{
  "name": "@agentforge/bridge",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "bun build src/server.ts --compile --outfile=bridge",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.2.81",
    "hono": "^4.7.0",
    "ws": "^8.18.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/ws": "^8.18.0",
    "@types/bun": "^1.3.2",
    "typescript": "^5.9.0"
  }
}
