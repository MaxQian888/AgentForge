---
name: React
description: Build and refine React component flows for AgentForge surfaces.
requires:
  - skills/typescript
tools:
  - code_editor
  - browser_preview
---

# React

Use this skill when a role needs focused React and App Router implementation guidance inside the current repository.

## Runtime Focus

- Prefer Server Components unless the interaction boundary clearly requires `"use client"`.
- Reuse the existing layout, state, and translation seams before introducing new abstractions.
- Keep responsive behavior, accessibility, and loading states intact across dashboard surfaces.
