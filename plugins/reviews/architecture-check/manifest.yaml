apiVersion: agentforge/v1
kind: ReviewPlugin
metadata:
  id: architecture-check
  name: Architecture Check
  version: 0.1.0
  description: Built-in review plugin that checks for architecture boundary drift and layering violations.
  tags:
    - builtin
    - review
    - architecture
spec:
  runtime: mcp
  transport: stdio
  command: bun
  args:
    - run
    - src/index.ts
  review:
    entrypoint: review:run
    triggers:
      events:
        - pull_request.updated
      filePatterns:
        - src/**/*.ts
        - src-go/**/*.go
    output:
      format: findings/v1
permissions: {}
source:
  type: builtin
  path: ./plugins/reviews/architecture-check/manifest.yaml
